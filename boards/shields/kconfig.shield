# No whitespace after the comma or in your part name!
config SHIELD_theduplex_LEFT
    def_bool $(shields_list_contains,theduplex_left)

# No whitespace after the comma or in your part name!
config SHIELD_theduplex_RIGHT
    def_bool $(shields_list_contains,theduplex_right)
